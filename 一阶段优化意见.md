# 一阶段优化意见

> 状态：讨论中 | 最后更新：2026-05-23
>
> 按主题分节讨论，✅ 标记为已确认，待全部确认后进入实施。

---

## 一、触发流程重构 ✅ 已确认

### 1.1 核心决策：仿王者荣耀大厅入口

**当前问题：** 应用启动直接强制跳转 Login 面板，不符合游戏 UI 惯例。

**重构方向（已确认）：**

```
启动 → Home (游戏主页/大厅门口)
          ├── [点击登录]  → Login (弹窗)  → 登录成功自动关闭回到 Home (已登录态)
          │                   └── [注册入口] → Register (弹窗) → 完成回到 Login
          ├── [离线模式]  → Lobby (受限：仅视频/帮助/设置)
          ├── [设置]      → Settings → 返回 Home
          ├── [帮助]      → Help → 返回 Home
          └── [进入大厅]  → Lobby (已登录，全功能)
                              ├── [开始学习] → SkillSelect → SceneSelect → Training → ScoreReport
                              ├── [成绩查看] → ScoreReport
                              └── [导学视频] → StudyVideo
```

### 1.2 GameState 调整

```csharp
public enum GameState
{
    Home,           // 新增：游戏主页（启动入口，替代原 Login 为入口）
    Lobby,          // 新增：游戏大厅（替代原 MainMenu）
    SceneSelect,    // 保留
    SkillSelect,    // 保留
    StudyVideo,     // 保留
    Training,       // 保留（待实现面板）
    Paused,         // 保留（待实现）
    ScoreReport,    // 保留
    Settings,       // 保留
    Help,           // 保留
    Login,          // 保留，从 FullScreen 降级为 Popup
    Register        // 新增：注册页（弹窗）
}
```

### 1.3 导航语义确认

| 语义 | 方法 | 行为 | 场景 |
|------|------|------|------|
| **Push** | `NavigateTo(state)` | 压栈后跳转 | Home→Settings, Lobby→SkillSelect |
| **Replace** | `ReplaceTo(state)` | 替换当前，不压栈 | Login→Register, Home(离线)→Lobby |
| **Back** | `GoBack()` | 出栈返回 | 所有面板的"返回"按钮 |
| **Popup** | `ShowPopup(state)` | 叠加显示，不隐藏底层 | Login/Register 弹窗 |

### 1.4 离线模式限制

离线模式下 Lobby 功能限制：

| 功能 | 离线 | 已登录 |
|------|------|--------|
| 开始学习（训练） | ❌ 不可用（点击提示登录） | ✅ |
| 成绩查看 | ❌ 不可用 | ✅ |
| 导学视频 | ✅ | ✅ |
| 设置 | ✅ | ✅ |
| 帮助 | ✅ | ✅ |

### 1.5 待办：Home 面板新建 + Login 面板改造

| 事项 | 类型 | 说明 |
|------|------|------|
| HomePanel | 新建 | 游戏主页，含登录按钮、离线按钮、设置、帮助入口 |
| LobbyPanel | 改造 | 当前 HomeMenuPanel，改名为 LobbyPanel |
| LoginPanel | 改造 | 从全屏降为弹窗，增加关闭按钮/点击外部关闭 |
| RegisterPanel | 新建 | 注册面板（弹窗），确认密码字段 |

---

## 二、代码架构优化（当前讨论主题）

### 2.1 GameStateMachine 清理 ✅ 已确认

**决定：** 删除 `GameStateMachine.cs`，状态追踪统一由 UIManager 管理。

### 2.2 AutoBind 重复代码 ✅ 已确认

**决定：** 方案 C（编辑器自动绑定为主 + BasePanel 基类兜底）。

- Editor 脚本在保存时自动填充 `[SerializeField]` 字段（项目已有类似能力）
- `BasePanel` 提供 `Bind<T>(string name)` 泛型方法，兜底运行时未绑定的引用

### 2.3 UIManager 职责拆分 ✅ 已确认

**决定：** 拆分为三个模块，UIManager 保留为对外唯一入口。

```
UIManager (单例入口)
  ├── PanelRegistry      — 面板注册/查找/生命周期（OnEnter/OnExit）
  ├── NavigationStack    — Push/Replace/Back 状态历史
  └── PanelLayerManager  — FullScreen / Popup / Toast 层级
```

---

## 三、用户体验优化（当前讨论主题）

### 3.1 过渡动画 ✅ 已确认

**决定：** 方案 B（Unity Animator），使用 AnimationClip 控制面板切换动画。

| 动画 | 场景 | 时长 |
|------|------|------|
| Fade Out → Fade In | FullScreen 面板切换 (Home↔Lobby) | 0.25s |
| Scale 0→1 + Fade In | Popup 打开 (Login/Register) | 0.2s |
| Slide Up + Fade Out | 关闭弹窗 | 0.15s |

每个 Panel 根节点挂 Animator + 对应 AnimationClip，由 PanelLayerManager 在切换时触发。

### 3.2 Settings Apply/Cancel 模式 ✅ 已确认

**决定：** 加入 Apply/Cancel 模式。

```
拖动滑块 → 临时变量缓存 → 预览生效
[确认]   → PlayerPrefs 写入 → 持久化
[取消/返回] → 恢复原始值 → 不写入
```

### 3.3 面板生命周期抽象 ✅ 已确认

**决定：** 引入 `BasePanel`，提供 `OnEnter(object data)` / `OnExit()` / `OnBack()` 生命周期。

---

## 四、实施优先级 & 收尾项（当前讨论主题）

### 4.1 实施顺序建议

| 优先级 | 事项 | 依赖 | 说明 |
|--------|------|------|------|
| **P0** | GameStateMachine 删除 | 无 | 1 个文件删除 |
| **P0** | 新建 HomePanel + 改造 LoginPanel 为弹窗 | 无 | 入口重构 |
| **P0** | 新建 RegisterPanel | LoginPanel | 注册流程 |
| **P1** | NavigationStack + PanelLayerManager | P0 | 导航栈和层级 |
| **P1** | BasePanel + AutoBind 编辑器 | P0 | 基类和自动绑定 |
| **P1** | 改造现有 7 个 Panel 继承 BasePanel | P1 | OnEnter/OnExit/OnBack |
| **P2** | 过渡动画 (Animator) | P1 | 每个 Panel 加 Animator |
| **P2** | Settings Apply/Cancel | P1 | SettingsPanel 改造 |

### 4.2 收尾项

- **Training 黑屏问题 ✅**：使用占位面板，待阶段二 TrainingHUD 替换
- **CLAUDE.md 同步更新 ✅**：精简 CLAUDE.md，加入文件索引指向 UI指导.md、一阶段优化意见.md

### 4.3 待确认

1. Training 黑屏暂用占位面板还是重定向到 StudyVideo？
2. 还有没有其他想加入一阶段优化的议题？
