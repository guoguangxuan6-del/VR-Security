# 项目问题诊断报告

- **生成时间**：2026-07-20 13:44 (GMT+8)
- **项目**：VR-Security（Life Guard / Subway Terminal, Unity HDRP）
- **当前分支**：`main` @ `d33fcd3`（= `origin/main`，已含 VR-Connect / NPC / 坍塌救援 / 好场景材质 / 最新 UI）
- **诊断方法**：只读核验。实时校验走本地 UnitySkills 服务（8090 端口，v2.1.3）；基线归属通过 `git` 对比 `origin/main` 确认。
- **说明**：本报告只做「定位 + 不修复的后果」，不含修复方案（按你要求）。

---

## 一、已确认健康（无需担心）

| 项 | 结论 |
|----|------|
| C# 编译 / 控制台错误 | **0 错误**（仅 1 条无害警告，见问题 5） |
| 缺失引用（`validate_missing_references`） | **0** |
| `Demonstration.unity` 好场景材质 | 与好版本 `0f9c9de` **逐字节一致**，未被覆盖 |
| 最新 UI（`UIManager.cs`） | 含你的本地改动（6 行），且已进 `main` |
| XR 包体系 | 已对齐 `com.unity.xr.openxr: 1.14.3` |
| 当前打开场景 XR 装置 | `xr_check_setup` 通过（XRI 已装、Main Camera 存在） |

---

## 二、问题清单（按严重度排序）

### 🔴 P1 — 50 个 HDRP/Unreal 着色器编译失败

- **定位**：`Assets/Shaders/*.shader`（共 50 个，内部名前缀 `Unreal/`，如 `BasicShapeMaterial`、`MI_Bin_01a`、`MI_Ceiling_Subway_01a`、`MI_Ads_Panel_01a`、`MI_CineMat_CameraBody` 等），全部位于 `Assets/Shaders/` 目录。
- **证据**：
  - `validate_shader_errors` → `count: 50`，每个着色器报 **9–12 个编译错误**。
  - 源码特征：着色器属性大量引用 HDRP 专属类型（如第 34 行 `UnityEditor.Rendering.HighDefinition.OpaqueCullMode`），确认这是「Unreal 材质转 HDRP」产物，与本项目 HDRP 管线一致，但**与当前安装的 HDRP 包版本不兼容**（Unity 2022.3.62f3 / HDRP 14.x），导致批量编译失败。
  - 这些 `.meta` 的 GUID 前缀为 `abc0000000000…`（Unity 内置/占位命名空间），属非典型导入，进一步印证来源异常。
- **影响面**：抽样 6 个着色器，每个被约 **1 个材质**引用（如 `MI_Bin_01a` 1 个、`MI_Ceiling_Subway_01a` 1 个）；推断 ~50 个材质受影响，多为地铁环境道具（垃圾桶、天花板、广告面板、摄影机机身等）。
- **不修复的后果**：
  - 使用这些材质的网格在运行时/编辑器中显示为**错误材质（洋红/粉）或回退着色**，场景视觉效果破损。
  - 控制台被 50×9~12 条着色器错误刷屏，淹没真实问题。
  - 出包（Build）时这些着色器不会阻断构建（默认降级为错误着色器），但**成品画面存在明显渲染缺陷**；若开启严格着色器校验则可能中断构建。
- **基线归属**：✅ 在 `origin/main` 中**固有**，本次合并**未引入、未改动**（git 已确认 `Assets/Shaders/` 与 main 一致）。属历史遗留。

### 🔴 P1 — `GameStateMachine 2.prefab` 缺失脚本（Missing Script）

- **定位**：`Assets/Prefabs/UI/GameStateMachine 2.prefab`，GameObject `GameStateMachine 2`。
- **证据**：`validate_find_missing_scripts` → `totalFound: 1`，`missingCount: 1`（1 个 MonoBehaviour 引用为空，即脚本被重命名/移动/删除后未回填）。
- **不修复的后果**：
  - 该 GameObject 丢失对应行为脚本，**游戏状态机逻辑缺位**。
  - 其他代码调用该脚本方法时会触发 **NullReferenceException / 运行时异常**，可能导致流程卡死或功能不可用。
  - 进入 Play Mode 或构建时 Unity 持续提示「Missing Script」。
- **基线归属**：✅ 在 `origin/main` 中**固有**，合并未改动。属历史遗留。

### 🟡 P2（风险，未确认是否破）— OpenXR 设置与构建场景未做编辑器内最终确认

- **定位**：`Packages/manifest.json`、`ProjectSettings/EditorBuildSettings.asset`、`Assets/XR/Settings/*`。
- **背景**：合并 PR #1 时，main 原 `EditorBuildSettings.m_Scenes` 为**空**、且 openxr settings 资产 GUID 与本地旧版不同（`16cc24b3` vs `8d01ee`）。文本合并已补回 Login + Training 构建场景并对齐 openxr 1.14.3，但 **Unity 资源冲突按规范应在编辑器内最终确认**（CLAUDE.md「VR XR anchors」）。
- **不确认的后果（风险）**：
  - 若 XR Plugin Management / OpenXR 设置在编辑器内未正确加载，VR 初始化可能失败（头显无画面、控制器无输入）。
  - 若构建场景列表在编辑器中被 Unity 重置，出包可能漏场景或构建报错。
- **状态**：当前无编译错误、无冲突标记残留，属「需人工肉眼/编辑器复核」项，不是已确认的 bug。

### 🟡 P2（卫生习惯）— 已合并的旧功能分支仍留存

- **定位**：`VR-Connect`（`6e65b31`）、`feature/npc-expression-animation`（`6e00d87`），两者均已合入 `main` 且**落后于 main**。
- **不处理后果**：
  - 仓库分支 clutter，易误用旧分支开发导致重复合并冲突。
  - 与 CLAUDE.md「功能分支开发、不在 main 直接开发」的规范不冲突，但增加维护成本。
- **注**：本机安全分支 `feature/core-training.before-main-merge`、`.merged-attempt` 已按你要求清理。

### 🟢 P3（无害）— 控制台空目录警告

- **定位**：`Assets/Editor` 空目录生成的无害 meta 提示。
- **后果**：无功能影响，可忽略。

---

## 三、汇总

| 严重度 | 问题 | 位置 | 是否本次引入 | 性质 |
|--------|------|------|--------------|------|
| 🔴 P1 | 50 个 HDRP/Unreal 着色器编译失败 | `Assets/Shaders/*.shader` | 否（main 基线） | 渲染缺陷 |
| 🔴 P1 | `GameStateMachine 2.prefab` 缺失脚本 | `Assets/Prefabs/UI/GameStateMachine 2.prefab` | 否（main 基线） | 运行时逻辑缺陷 |
| 🟡 P2 | OpenXR 设置 / 构建场景未编辑器确认 | `Packages/`、`ProjectSettings/`、`Assets/XR/` | 合并引入（需确认） | 风险待核实 |
| 🟡 P2 | 旧功能分支未清理 | `VR-Connect`、`feature/npc-expression-animation` | 否 | 仓库卫生 |
| 🟢 P3 | 空目录警告 | `Assets/Editor` | 否 | 无害 |

## 四、结论

1. **本次合并（PR #1）未向 `main` 引入任何新问题**——所有 P1 缺陷均为 `origin/main` 的历史基线，合并前后一致。
2. **当前 `main` 的「好场景材质 + 最新 UI + VR-Connect」目标已达成且经校验**。
3. **真正需要关注的两块**：(a) 50 个 HDRP/Unreal 着色器的版本不兼容；(b) `GameStateMachine 2.prefab` 的缺失脚本——二者都会在实际运行/出包时暴露，建议单独立项处理。
4. **OpenXR 设置与构建场景**需在 Unity 编辑器内做一次最终确认，以排除 P2 风险。

> 本报告刻意不包含修复方案。如需要，可另行要求对任一项给出定位后的处理建议。
